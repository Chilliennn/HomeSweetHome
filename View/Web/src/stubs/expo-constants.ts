// Stub for expo-constants on web
// Environment variables are now handled via vite.config.ts define (VITE_* -> process.env.EXPO_PUBLIC_*)
// This stub only exists for alias resolution in case any dependency imports expo-constants

export default {
    expoConfig: {
        extra: {},
    },
    manifest: {
        extra: {},
    },
};
